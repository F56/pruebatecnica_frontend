import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({
  closeButton: { float: "right" },
  sidebarList: { width: "80vw", padding: "0 1.5rem" }
}));

export default useStyles;